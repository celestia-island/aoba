use eframe::egui;
use egui::Ui;

// lang is not needed here; status UI is currently empty

/// Render bottom status content into an existing bottom panel UI.
pub fn render_status_ui(ui: &mut Ui) {
    ui.horizontal(|_ui| {});
}
